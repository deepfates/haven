# Runtime Load Smoke

Date: 2026-07-01

Commit under test: working tree after `097359a7`

URL: http://127.0.0.1:4000/

Command:

```bash
MIX_ENV=dev mix haven.runtime_smoke --load-runs 3 --timeout-ms 30000
```

Result:

- Dev database gate reported `No pending migrations.`
- Base runtime smoke passed.
- Additional load smoke created 3 disposable runs.
- The load smoke alternated ordinary echo and bounded long-output turns.
- Rendered run pages included their own run titles and expected output markers.
- Rendered run pages did not include other load-run titles or ids.
- Reloaded inbox contained the created load-run rows.

Primary smoke run:

- run id: `3c55080b-a4bf-4073-9f8a-b35dd970a5f4`
- run URL:
  `http://127.0.0.1:4000/runs/3c55080b-a4bf-4073-9f8a-b35dd970a5f4`

Outcome: pass.

Scope note: this is dev-server evidence against the local ACP stub. It makes
multi-run and bounded long-output behavior easier to falsify, but it does not
prove realistic external-agent load.
